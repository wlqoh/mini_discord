// Package storage is an empty parent package for the storage
// implementations in its subdirectories (postgresql, cache, objectStorage,
// single_flight); it declares no symbols of its own.
package storage
